#include macros.dta
#ifdef HX_XBOX
#define TEST
('/*one*/' two '/*three*/')
#else
#define TEST
(two)
#endif
(sysutl
   (precache
      (dirs SYSTEM_DIRS)
      (exclude
         "facefx"
         "gen")
      (exclude_rnd
         "test")
      (platforms ps2)
      #ifdef HX_EE
      (shaders
         (enabled FALSE))
      #endif)
   (test_script
      {print
         TEST
         "\n"}))
(mem
   (disable_mgr TRUE))
(timer precache anims bitmap bank dta rnd wav)
#merge default.dta